# Docs

- Phase 1 services: api-gateway, auth-service, user-service, catalog-service
- See service-level READMEs for API details and env vars
- Docker Compose manifests live in `deploy/docker-compose/`
